# Base URL of the Esscentra backend API.
# Registration (POST /auth/register) calls this live backend.
# All other auth screens currently still use the in-browser mock.
VITE_API_BASE_URL=http://localhost:4000/api/v1
